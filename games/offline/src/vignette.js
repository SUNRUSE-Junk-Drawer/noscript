var vignetteProgram = compileGlsl(
  vector(
    float(0),
    float(0),
    float(0),
    subtract(
      float(1),
      pow(
        divide(
          float(1),
          divide(
            length(uv),
            float(6)
          )
        ),
        float(6)
      )
    )
  )
)
